#include <iostream>
#include <vector>
using namespace std;

int main(void) {
	int N, K;
	cin >> N >> K; 
	vector<int> w, v;
	for (int i = 0; i < N; i++) {
		int W, V;
		cin >> W >> V;
		w.push_back(W);
		v.push_back(V);
	}
	for (int i = 0; i < N; i++) {

	}
}